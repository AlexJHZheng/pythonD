#1.安装软件
$ sudo apt install softname

#2.卸载软件
$ sudo apt remove softname

#3.更新可用软件包列表
$ sudo apt update

#4.更新已安装的包
$ sudo apt upgrade

#5.软件列表
$ sudo apt ipython

#6.deb安装格式
$ sudo dpkg -i <package.deb> 

==============================================================
Lunix 命令
1. ls    列出文件夹下目录
      -a显示隐藏文件
      通配符 *任意 ？一个字符   []  定义一个匹配的字符组 bn
 
